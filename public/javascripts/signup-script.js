$(document).ready(function(){

    // Hide header, footer and change the body bg
    $('footer').css({"display":"none"});
    $('header').css({"display":"none"});
    // $('body').css({"background":"rgb(9, 19, 49)"});

    // trigger click on the google btn
    // $('.google-btn').click(function(){
    //     $('#google-link').trigger('click');
    // });

});